[project]
name = "pdf-ocr-tool"
version = "0.1.0"
description = "DeepSeek-OCR PDF processing backend for OpenCode"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.0.0",
    "PyMuPDF>=1.23.0",
    "Pillow>=10.0.0",
]

[project.scripts]
pdf_ocr_backend = "tool.pdf_ocr_backend:main"
